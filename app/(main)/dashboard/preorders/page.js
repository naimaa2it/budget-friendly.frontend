import PreordersList from '@/components/dashboard/Order/PreordersList';

export const metadata = { title: 'Pre-orders — Dashboard' };

export default function PreordersPage() {
  return <PreordersList />;
}
