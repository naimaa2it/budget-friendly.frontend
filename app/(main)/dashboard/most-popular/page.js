import MostPopular from '@/components/dashboard/Analytics/MostPopular';

export const metadata = { title: 'Most Popular — Dashboard' };

export default function MostPopularPage() {
  return <MostPopular />;
}
